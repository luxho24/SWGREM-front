/**Broma, Eliminar esta carpeta**/

import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Importar el archivo CSS
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
